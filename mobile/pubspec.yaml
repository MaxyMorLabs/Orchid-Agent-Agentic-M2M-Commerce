name: orchid_watcher
description: Orchid Agent Watcher - push notification monitor for M2M trades
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: "^3.1.0"
  firebase_messaging: "^15.0.2"
  http: "^1.2.1"
  flutter_local_notifications: "^17.1.2"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: "^4.0.0"

flutter:
  uses-material-design: true

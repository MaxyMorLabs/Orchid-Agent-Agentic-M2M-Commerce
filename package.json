{
  "name": "orchid-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "test:contracts": "cd contracts/escrow && cargo test",
    "test": "npm run test:backend && npm run test:frontend",
    "dev": "docker-compose up --build"
  }
}
